const qtdLinha = Number(prompt('Quantas linhas vao ter cada Matriz?'))
const qtdColunas = Number(prompt('Quantas colunas vao ter cada Matriz?'))


function criaMatrizes (linhas, colunas) {
    let matrizA = new Array()
    let matrizB = new Array()

    for (let i = 0; i < linhas; i++) {
        for (let j = 0; j < colunas; j++) {

        }
    }
}

criaMatrizes(qtdLinha, qtdColunas)